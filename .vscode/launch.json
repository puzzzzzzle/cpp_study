{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug (CMake 当前目标)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "environment": [],
      "MIMode": "gdb",
      "preLaunchTask": "Build (CMake 当前项目)"
    },
    {
      "name": "Debug (CMake 当前目标 + 自定义参数)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": ["--gtest_filter=SuiteName.CaseName"],
      "cwd": "${workspaceFolder}",
      "environment": [],
      "MIMode": "gdb",
      "preLaunchTask": "Build (CMake 当前项目)"
    }
  ]
}
